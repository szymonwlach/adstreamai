# name: Refresh n8n on AWS

# on:
#   push:
#     branches: ["main"]

# env:
#   AWS_REGION: eu-central-1
#   ECS_CLUSTER: adstreamai-cluster
#   ECS_SERVICE: n8n-service-v6 # Zmienione na Twoją aktualną nazwę usługi

# jobs:
#   deploy:
#     name: Trigger ECS Redeploy
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Force ECS Update
#         run: |
#           # Ta komenda mówi AWS: "Hej, pobierz najnowszy obraz n8n i zrestartuj kontener"
#           aws ecs update-service --cluster ${{ env.ECS_CLUSTER }} --service ${{ env.ECS_SERVICE }} --force-new-deployment
